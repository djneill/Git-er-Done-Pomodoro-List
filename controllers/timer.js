module.exports = {
    timer: function handleTimerForm(req, res) {
        // TODO: Get the timer duration from the request body
        // TODO: Start the timer
        res.redirect('/todos');
      }
}